#! Description: Enable/Disable SNMP traps for interface state changes.
#! Prompt: [{question_name: Enable/Disable Link-Change Traps@colon, required: true, type: multiplechoice, variable: prompt_snmp_intchange, default: Disable, choices: Enable\nDisable}, {question_name: Interface or Range@colon, question_description: (e.g. eth 1/1/1 or eth 1/1/2 to 1/1/4 for range), required: true, type: text, variable: prompt_snmp_intchange_int, min: 5, max: 1024}]
# =====================
---
- hosts: all
  connection: network_cli
  gather_facts: no
  vars:
    ansible_network_os: icx
    ansible_become: true
    ansible_become_method: enable
    NewlineVar: "\r"
    ErrParse: '^.*ConnectionError: '
    EthRe: '(ethernet|etherne|ethern|ether|ethe|eth|et|e)'
    PortRe: '([0-9]{1,2}/[0-9]{1,2}/[0-9]{1,2})'
    RngRe: '^.*([0-9]{1,2}/[0-9]{1,2}/[0-9]{1,2}).*to.*([0-9]{1,2}/[0-9]{1,2}/[0-9]{1,2}).*$'

  tasks:
  - name: Setup for Trap Disable
    set_fact: {TrapDisable: "no snmp-server"}
    when: "'Disable' in prompt_snmp_intchange"

  - name: Format Interface List
    set_fact: {EthInt: '{{tEthInt|regex_replace(PortRe, "e \1")}}'}
    vars: {tEthInt: '{{prompt_snmp_intchange_int|regex_replace(EthRe)}}'}
    when: "'to' not in prompt_snmp_intchange_int"

  - name: Format Interface Range
    set_fact: {EthInt: '{{prompt_snmp_intchange_int|regex_replace(RngRe, "e \1 to \2")}}'}
    when: "'to' in prompt_snmp_intchange_int"

  - name: Apply SNMP Trap Configuration
    icx_config:
      lines: '{{TrapDisable|default("snmp-server", true)}} enable traps link-change'
      save_when: changed
      parents: interface {{EthInt}}
    register: IcxCfgStatus
    failed_when: IcxCfgStatus.msg is defined and "MODULE FAILURE" not in IcxCfgStatus.msg

  - name: Parse Switch Error
    set_fact:
      ErrMsg: "{{IcxCfgStatus.module_stderr|regex_replace('\n')|regex_replace(ErrParse)}}"
    when: IcxCfgStatus.rc is defined and IcxCfgStatus.rc == 1

  - name: Display Switch Error
    assert: {that: ErrMsg is not defined, fail_msg: '{{ErrMsg.split(NewlineVar)}}'}
    when: ErrMsg is defined
